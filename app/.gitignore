/_build
/cover
/deps
/doc
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/
/assets/node_modules/
node_modules/

# Generated web assets — bundler output (esbuild/tailwind) + mix phx.digest
# artifacts. Never commit; regenerated by the dev watchers and the
# release/Docker asset build.
/priv/static/assets/
/priv/static/cache_manifest.json
/priv/static/**/*.gz

# Load benchmark report artifacts (regenerated by `mix flux.benchmark`).
# The directory is tracked (via .gitkeep) but result files are not.
/bench/results/*.md
